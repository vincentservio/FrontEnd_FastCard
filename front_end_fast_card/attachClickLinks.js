const attachClickToDeck = () => {
  let cards = document.querySelectorAll("li a");
  cards.forEach((card) => {
    card.addEventListener("click", () => {
      // debugger;
    });
  });
};

const viewCard = () => {
  debugger;
};
